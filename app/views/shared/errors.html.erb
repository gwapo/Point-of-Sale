  <% if @receiving.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@receiving.errors.count, "error") %> prohibited this receiving from being saved:</h2>

      <ul>
      <% @receiving.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

